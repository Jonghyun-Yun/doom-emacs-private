# -*- mode: snippet; require-final-newline: nil -*-
# name: foreach
# key: foreach
# binding: direct-keybinding                     
# --
foreach(${1:index} = ${2:vector}, .combine ='${3:list}') %dopar% {
    $0
}
